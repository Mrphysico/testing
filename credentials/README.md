# Login Details

Use `private-login-details.md` in this folder to keep local development login details.

The file is ignored by Git, so local passwords are not published to GitHub.

Public family-account passwords are not stored in readable form by the application. Each family member chooses a password during sign-up. An administrator can reset a password from the Admin Panel when needed.
